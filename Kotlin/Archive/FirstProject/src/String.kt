


fun main (args:Array<String>) {

    var title = "Welcome to Kuala Lumpur";
    println(title);
    println("title:"+title);
    println("title:$title");
    println("title Capital:" + title.toUpperCase());
    println("title Lower:" + title.toLowerCase());
    println("title Lower:" + title.split(" ")); // split string by spaces and turn into a array
    println("title Lower:" + title.trim()); // remove a single space, in this case the oolon is removed


    var name = "yap" + "sammi";
    println("name:$name");
    println("Second Char:" + title[1]);







}