


fun main (args:Array<String>)   {

    for (item in 1..10) // kotlin supports this shortcut ,so rather than typing for (int item =1; item<=10; item++), you just type this in 1..10
    {
//        if (item==2)
        println ("count:$item");
    }

    println ("loop done");




}